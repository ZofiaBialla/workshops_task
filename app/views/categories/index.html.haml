.row
  - categories.each do |category|
    .col-sm-6.col-md-4
      = link_to category do
        .thumbnail
          = image_tag category.pictureurl 
          .caption
            %h3= category.name
            -if user_signed_in? && current_user.admin?
              %hr
              .row
                .col-xs-8
                .col-xs-4
                  %b= link_to 'edit', edit_category_path(category)
                  \|
                  %b= link_to ' delete', category, method: :delete, data: { confirm: 'Are you sure?' }

-if user_signed_in? && current_user.admin?
  = link_to 'New Category', new_category_path
